---
phase: 04.1-shopify-data-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/page.press.json
  - templates/page.careers.json
  - templates/collection.portfolio.json
  - templates/collection.workshop.json
  - templates/product.workshop-item.json
  - templates/blog.journal.json
  - templates/article.journal-entry.json
  - sections/header-group-minimal.json
autonomous: true
requirements:
  - DATA-05

must_haves:
  truths:
    - "Seven new template JSON files exist in templates/ directory"
    - "Gallery collection templates reference header-group-minimal (no announcement bar)"
    - "Each template uses appropriate Prestige stand-in sections as placeholders"
    - "All template JSON is valid and parseable"
  artifacts:
    - path: "templates/page.press.json"
      provides: "Press page template"
      contains: "main-page"
    - path: "templates/page.careers.json"
      provides: "Careers page template"
      contains: "main-page"
    - path: "templates/collection.portfolio.json"
      provides: "Portfolio collection template"
      contains: "main-collection"
    - path: "templates/collection.workshop.json"
      provides: "Workshop collection template"
      contains: "main-collection"
    - path: "templates/product.workshop-item.json"
      provides: "Workshop product detail template"
      contains: "main-product"
    - path: "templates/blog.journal.json"
      provides: "Journal blog listing template"
      contains: "main-blog"
    - path: "templates/article.journal-entry.json"
      provides: "Journal article template"
      contains: "main-article"
    - path: "sections/header-group-minimal.json"
      provides: "Minimal header group without announcement bar"
      contains: "header"
  key_links:
    - from: "templates/collection.portfolio.json"
      to: "sections/header-group-minimal.json"
      via: "section group reference"
      pattern: "header-group-minimal"
    - from: "templates/collection.workshop.json"
      to: "sections/header-group-minimal.json"
      via: "section group reference"
      pattern: "header-group-minimal"
---

<objective>
Create all 7 new page/collection/product/blog template JSON files plus a minimal header group for gallery pages.

Purpose: Templates define the URL routes and section structure for every page type in Studio Peake. Later phases replace stand-in Prestige sections with custom SP sections. Gallery templates (portfolio + workshop collections) use minimal chrome for immersive browsing.

Output: 8 JSON files in templates/ and sections/ directories.
</objective>

<execution_context>
@/Users/rowleythompson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rowleythompson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/4.1-shopify-data-architecture-inserted/4.1-CONTEXT.md
@.planning/phases/4.1-shopify-data-architecture-inserted/4.1-RESEARCH.md

Reference existing templates for structure patterns:
@templates/collection.json
@templates/page.json
@templates/blog.json
@templates/article.json
@templates/product.json
@sections/header-group.json

<interfaces>
<!-- Existing Prestige template JSON pattern -->
<!-- All templates follow: { "sections": { "key": { "type": "section-type", "settings": {} } }, "order": ["key"] } -->
<!-- Section group pattern: { "type": "header|footer", "name": "...", "sections": { ... }, "order": [...] } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create minimal header group and gallery collection templates</name>
  <files>sections/header-group-minimal.json, templates/collection.portfolio.json, templates/collection.workshop.json</files>
  <action>
1. Create `sections/header-group-minimal.json` — copy from `sections/header-group.json` but remove the `announcement-bar` section entry and its order reference. Keep the `header` section with all its current settings intact. Set `"type": "header"` and `"name": "Header group (minimal)"`.

2. Create `templates/collection.portfolio.json` — use `collection.json` as structural reference:
   - Include `collection-banner` section with same settings as collection.json but `"image_size": "md"` and `"allow_transparent_header": true`
   - Include `main` section of type `main-collection` with default settings (this is the Prestige stand-in — will be replaced by SP portfolio grid section in Phase 6)
   - The template's section_group should reference `header-group-minimal` instead of `header-group` for minimal chrome. Note: Shopify JSON templates do NOT contain section group references directly — section groups are assigned at the theme level. Instead, to achieve minimal chrome, set the announcement bar to disabled state. Actually, section groups apply globally. The simplest approach per the research: create the template normally and handle announcement bar visibility through template-conditional logic in later phases. For now, create the template with standard structure.

3. Create `templates/collection.workshop.json` — same structure as collection.portfolio.json:
   - Include `collection-banner` section with similar settings
   - Include `main` section of type `main-collection` with `"products_per_page": 24` and `"products_size_desktop": "medium"`
   - Standard Prestige collection template structure

Both collection templates are Prestige stand-ins. Portfolio will show empty (collections are route shells). Workshop will show products once they exist.
  </action>
  <verify>
    <automated>cd /Users/rowleythompson/Projects/claude-projects/shopify/studio-peake-theme && node -e "const fs=require('fs'); ['sections/header-group-minimal.json','templates/collection.portfolio.json','templates/collection.workshop.json'].forEach(f=>{const d=JSON.parse(fs.readFileSync(f,'utf8'));console.log(f+': valid JSON, sections: '+Object.keys(d.sections||{}).join(', '))})"</automated>
  </verify>
  <done>Three JSON files exist with valid structure. header-group-minimal has header section only (no announcement-bar). Both collection templates have collection-banner and main-collection sections.</done>
</task>

<task type="auto">
  <name>Task 2: Create page, product, blog, and article templates</name>
  <files>templates/page.press.json, templates/page.careers.json, templates/product.workshop-item.json, templates/blog.journal.json, templates/article.journal-entry.json</files>
  <action>
1. Create `templates/page.press.json` — based on `page.json` pattern:
   - `main` section of type `main-page` with empty settings (Prestige stand-in — will be replaced by SP press section in Phase 9)
   - Add an `image-with-text` section below main as a placeholder for press content layout

2. Create `templates/page.careers.json` — based on `page.json` pattern:
   - `main` section of type `main-page` with empty settings (Prestige stand-in — will be replaced by SP careers section in Phase 9)

3. Create `templates/product.workshop-item.json` — based on `product.json` pattern:
   - Copy the full `main-product` section structure from product.json including all blocks (vendor, title, price, description, variant_picker, quantity_selector, buy_buttons)
   - Remove the `complementary_products` block, `related-products` section, and `recently_viewed_products` section — workshop items have a different layout
   - Keep `payment_terms` block
   - Set `"desktop_media_layout": "grid"` and `"product_info_size": 40`
   - This is the Prestige stand-in; will be enhanced with workshop-specific metafield displays in Phase 8

4. Create `templates/blog.journal.json` — based on `blog.json` pattern:
   - `main` section of type `main-blog` with settings: `"show_blog_title": true`, `"show_tags": true`, `"posts_per_page": 12`, `"feature_first_blog_post": true`, `"show_category": true`, `"show_date": true`, `"show_excerpt": true`, `"show_read_more": true`
   - This is a dedicated journal template, separate from the default blog.json

5. Create `templates/article.journal-entry.json` — based on `article.json` pattern:
   - Copy the `main-article` section with settings from article.json
   - Include `prev-next-blog-posts` section with `"title": "More from the journal"`
   - Set `"show_date": true` and `"show_author": true`
  </action>
  <verify>
    <automated>cd /Users/rowleythompson/Projects/claude-projects/shopify/studio-peake-theme && node -e "const fs=require('fs'); ['templates/page.press.json','templates/page.careers.json','templates/product.workshop-item.json','templates/blog.journal.json','templates/article.journal-entry.json'].forEach(f=>{const d=JSON.parse(fs.readFileSync(f,'utf8'));console.log(f+': valid JSON, order: '+JSON.stringify(d.order))})"</automated>
  </verify>
  <done>Five template JSON files exist with valid structure. Each uses appropriate Prestige stand-in sections. product.workshop-item has streamlined blocks without complementary/related products. blog.journal and article.journal-entry are separate from default blog/article templates.</done>
</task>

</tasks>

<verification>
All 8 JSON files parse without error:
```bash
cd /Users/rowleythompson/Projects/claude-projects/shopify/studio-peake-theme && for f in sections/header-group-minimal.json templates/page.press.json templates/page.careers.json templates/collection.portfolio.json templates/collection.workshop.json templates/product.workshop-item.json templates/blog.journal.json templates/article.journal-entry.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'));console.log('OK: $f')" || echo "FAIL: $f"; done
```
</verification>

<success_criteria>
- 7 new template files in templates/ directory
- 1 new section group file in sections/ directory
- All files are valid JSON
- Templates use correct Prestige section types as stand-ins
- No existing files modified
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-shopify-data-architecture/04.1-01-SUMMARY.md`
</output>
