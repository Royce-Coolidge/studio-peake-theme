---
phase: 04.1-shopify-data-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - .planning/phases/04.1-shopify-data-architecture/shopify-admin-setup.md
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-06

must_haves:
  truths:
    - "Four metaobject types exist in Shopify Admin with correct fields"
    - "Product metafield namespace workshop has all 7 field definitions"
    - "Six collections exist with correct types (manual for portfolio, smart for workshop)"
    - "Templates are assigned to appropriate pages and collections"
    - "Portfolio projects use metaobject entries (not products)"
    - "Sample metaobject entries exist for development testing"
  artifacts:
    - path: ".planning/phases/04.1-shopify-data-architecture/shopify-admin-setup.md"
      provides: "Step-by-step Admin setup guide with exact field specs"
      min_lines: 100
  key_links:
    - from: "Shopify Admin metaobject type: project"
      to: "templates/collection.portfolio.json"
      via: "Sections in portfolio template will query project metaobjects"
      pattern: "metaobjects.project"
    - from: "Shopify Admin metafield: workshop.*"
      to: "templates/product.workshop-item.json"
      via: "Product template sections will display workshop metafield values"
      pattern: "product.metafields.workshop"
    - from: "Smart collection conditions"
      to: "Shopify Admin metafield: workshop.collection_name"
      via: "Smart collections filter products by metafield value"
      pattern: "workshop.collection_name"

user_setup:
  - service: shopify-admin
    why: "Metaobject types, metafield definitions, collections, and content entries must be created in Shopify Admin"
    dashboard_config:
      - task: "Create 4 metaobject type definitions"
        location: "Shopify Admin > Settings > Custom data > Metaobjects"
      - task: "Create 7 product metafield definitions under workshop namespace"
        location: "Shopify Admin > Settings > Custom data > Products"
      - task: "Create 6 collections (4 manual portfolio, 2+ smart workshop)"
        location: "Shopify Admin > Products > Collections"
      - task: "Assign templates to pages and collections"
        location: "Shopify Admin > Pages/Collections > Theme template dropdown"
      - task: "Create sample metaobject entries and workshop products"
        location: "Shopify Admin > Content > Metaobjects / Products"
---

<objective>
Create a comprehensive Shopify Admin setup guide, then checkpoint for human to execute all Admin-side data architecture: metaobject types, metafield definitions, collections, template assignments, and sample content.

Purpose: The theme template files (Plan 01) define routes but are empty shells without the underlying Shopify data structures. This plan creates all Admin-side configuration that sections will query in later phases.

Output: Detailed setup guide document + verified Shopify Admin configuration.
</objective>

<execution_context>
@/Users/rowleythompson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rowleythompson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/4.1-shopify-data-architecture-inserted/4.1-CONTEXT.md
@.planning/phases/4.1-shopify-data-architecture-inserted/4.1-RESEARCH.md
@.planning/phases/04.1-shopify-data-architecture/04.1-01-SUMMARY.md

Content docs for populating data:
@copy/WORKSHOP (PRODUCT PAGES) - Website Copy Doc.md
@copy/PROJECT PAGES - Website Copy Doc.md
@copy/ABOUT - Website Copy Doc.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Shopify Admin setup guide from content docs and requirements</name>
  <files>.planning/phases/04.1-shopify-data-architecture/shopify-admin-setup.md</files>
  <action>
Read the three content docs in `copy/` to extract real data, then create a comprehensive setup guide at `.planning/phases/04.1-shopify-data-architecture/shopify-admin-setup.md` with these sections:

**Section 1: Metaobject Type Definitions**

For each of the 4 types, specify exact field names, types, and validation:

1. `project` metaobject type:
   - Display name: "Project"
   - Access: Storefronts (publishable capability ON, onlineStore OFF for now)
   - Fields:
     - `title` (single_line_text_field) — display name field
     - `category` (list.single_line_text_field) — e.g. ["residential", "hospitality"]
     - `hero_image` (file_reference, image only)
     - `gallery_images` (list.file_reference, images only)
     - `video_url` (url)
     - `description` (multi_line_text_field)
     - `press_link` (url)
     - `featured` (boolean) — controls homepage carousel inclusion

2. `press_accolade` metaobject type:
   - Display name: "Press Accolade"
   - Access: Storefronts
   - Fields:
     - `publication` (single_line_text_field) — display name field
     - `headline` (single_line_text_field)
     - `article_url` (url)
     - `date` (date)

3. `team_member` metaobject type:
   - Display name: "Team Member"
   - Access: Storefronts
   - Fields:
     - `name` (single_line_text_field) — display name field
     - `role` (single_line_text_field)
     - `bio` (multi_line_text_field)
     - `image` (file_reference, image only)
     - `anchor_id` (single_line_text_field) — for smooth-scroll anchor links

4. `career_opening` metaobject type:
   - Display name: "Career Opening"
   - Access: Storefronts
   - Fields:
     - `title` (single_line_text_field) — display name field
     - `specification` (rich_text_field)
     - `is_active` (boolean)

**Section 2: Product Metafield Definitions**

Under namespace `workshop`, list all 7 definitions with exact key, type, and description:
- `tearsheet_pdf` — file_reference (accepts: PDF)
- `secondary_image` — file_reference (accepts: images)
- `room_scheme_images` — list.file_reference (accepts: images)
- `interactive_tags` — json (document expected structure: `[{x_percent, y_percent, label, product_handle}]`)
- `video_url` — url
- `category` — single_line_text_field (for filtering)
- `collection_name` — single_line_text_field (for smart collection conditions)

**Section 3: Collections**

List all 6 collections with type and conditions:

Manual collections (empty route shells for portfolio):
- `portfolio-all` — Manual, empty, assign template: collection.portfolio
- `portfolio-residential` — Manual, empty, assign template: collection.portfolio
- `portfolio-commercial` — Manual, empty, assign template: collection.portfolio
- `portfolio-hospitality` — Manual, empty, assign template: collection.portfolio

Smart collections (workshop):
- `workshop-all` — Smart, condition: product metafield `workshop.category` is set (or product type = Workshop), assign template: collection.workshop
- `workshop-lighting` — Smart, condition: `workshop.collection_name` equals "lighting" (placeholder name, to be renamed), assign template: collection.workshop

**Section 4: Template Assignments**

Map each page/collection to its template:
- Pages: page.press for Press page, page.careers for Careers page (existing page.about and page.contact keep their templates)
- Collections: collection.portfolio for all portfolio-* collections, collection.workshop for all workshop-* collections
- Products: product.workshop-item for workshop products
- Blog: blog.journal for the Journal blog
- Articles: article.journal-entry for journal articles
- Note: index.json, page.about.json, page.contact.json already exist and keep current templates

**Section 5: Sample Content to Create**

Parse the content docs and list specific entries to create:
- Extract 3-4 project entries from `copy/PROJECT PAGES - Website Copy Doc.md` with real titles and descriptions
- Extract team members from `copy/ABOUT - Website Copy Doc.md` with real names and roles
- Extract workshop products from `copy/WORKSHOP (PRODUCT PAGES) - Website Copy Doc.md` with real product names and descriptions
- Create 2-3 placeholder press_accolade entries
- Create 1-2 placeholder career_opening entries

Format each entry as a clear checklist the human can follow in Shopify Admin.
  </action>
  <verify>
    <automated>cd /Users/rowleythompson/Projects/claude-projects/shopify/studio-peake-theme && test -f .planning/phases/04.1-shopify-data-architecture/shopify-admin-setup.md && wc -l .planning/phases/04.1-shopify-data-architecture/shopify-admin-setup.md | awk '{if($1 >= 100) print "OK: "$1" lines"; else print "FAIL: only "$1" lines, expected 100+"}'</automated>
  </verify>
  <done>Setup guide exists with 100+ lines covering all 5 sections. Every metaobject field, metafield definition, collection, and template assignment is specified with exact values. Sample content entries are listed with real data from content docs.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute Shopify Admin setup — metaobjects, metafields, collections, content</name>
  <files>Shopify Admin (no theme files modified)</files>
  <action>
Follow the setup guide at `.planning/phases/04.1-shopify-data-architecture/shopify-admin-setup.md` to create all data structures in Shopify Admin:

1. **Metaobject types** (Settings > Custom data > Metaobjects): Create all 4 types with specified fields. Enable "Storefronts" access on each type.
2. **Product metafield definitions** (Settings > Custom data > Products): Create all 7 definitions under namespace `workshop`.
3. **Collections** (Products > Collections): Create 4 manual portfolio collections (empty) and 2+ smart workshop collections with metafield conditions. Assign correct templates to each.
4. **Template assignments**: Create Press/Careers pages and Journal blog if needed. Assign templates per the guide.
5. **Sample content**: Create sample metaobject entries and workshop products following the guide. Mark as Active (published).
6. **Verify in Customiser**: Open a portfolio collection and workshop product to confirm templates load. Test dynamic source picker with a project metaobject.
  </action>
  <verify>
    <automated>echo "MANUAL VERIFICATION — Human confirms all Admin setup complete in Shopify Admin"</automated>
  </verify>
  <done>All 4 metaobject types, 7 metafield definitions, 6 collections, template assignments, and sample content exist in Shopify Admin. Dynamic source picker shows metaobject fields in Customiser.</done>
</task>

</tasks>

<verification>
After human completes Admin setup:
1. All 4 metaobject types visible in Settings > Custom data > Metaobjects
2. All 7 workshop metafield definitions visible in Settings > Custom data > Products
3. Six collections exist with correct templates assigned
4. Sample metaobject entries are Active and accessible
5. Customiser dynamic source picker shows metaobject fields
</verification>

<success_criteria>
- Setup guide document is comprehensive and actionable
- Human confirms all 4 metaobject types created with correct fields
- Human confirms all 7 product metafield definitions exist
- Human confirms 6 collections exist with correct templates
- Human confirms sample content entries are accessible in Customiser
- Metaobject references work in section schema dynamic source picker
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-shopify-data-architecture/04.1-02-SUMMARY.md`
</output>
